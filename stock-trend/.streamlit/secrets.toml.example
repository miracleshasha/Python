# 이 파일을 secrets.toml 로 복사한 뒤 실제 키를 넣으세요.
# (secrets.toml 은 .gitignore 처리되어 커밋되지 않습니다.)
#
# Streamlit Community Cloud에 배포할 때는 파일 대신
# 앱 설정 > Settings > Secrets 에 아래 내용을 붙여넣으면 됩니다.

DATA_GO_KR_API_KEY = "여기에_공공데이터포털_일반_인증키"

# 한국투자증권(KIS) Open API — 실시간/분봉 + 투자자 수급(클라우드 대응)
# KIS Developers(https://apiportal.koreainvestment.com)에서 발급
KIS_APP_KEY = "여기에_KIS_앱키"
KIS_APP_SECRET = "여기에_KIS_앱시크릿"
# KIS_ENV = "real"   # real(실전) | vps(모의). 기본 real
